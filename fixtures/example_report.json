[
  {
    "category": "correctness",
    "confidence": 0.64,
    "context": "@@ -1,3 +1,22 @@\n-def add(a, b):\n-    return a + b\n+def risky(a, b, c, d, e, f):\n+    TODO = \"replace before release\"\n+    try:\n+        x = a + b\n+        if x > 0:\n+            if c:\n+                if d:\n+                    if e:\n+                        if f:\n+                            return x\n+        return \"fallback\"\n+    except Exception:\n+        pass\n+    return None\n+\n+def duplicate():\n+    val = 1\n+    val = 1\n+    val = 1\n+    return val",
    "file": "src/example.py",
    "id": "dc7641726051",
    "location": {
      "end_line": 16,
      "start_line": 1,
      "symbol": "risky"
    },
    "message": "`risky` appears to return multiple unrelated types: none, object, string.",
    "recommendation": [
      "Normalize return type (single data shape or typed result object).",
      "Use exceptions for error paths instead of ad-hoc return values."
    ],
    "severity": "HIGH",
    "source": {
      "analyzer": "static",
      "pointer": null,
      "rule": "inconsistent_return_types"
    },
    "suggested_patch": null,
    "suggested_tests": [
      "Add tests asserting stable return type across success/error paths."
    ],
    "title": "Inconsistent return shapes"
  },
  {
    "category": "structure",
    "confidence": 0.76,
    "context": "@@ -1,3 +1,22 @@\n-def add(a, b):\n-    return a + b\n+def risky(a, b, c, d, e, f):\n+    TODO = \"replace before release\"\n+    try:\n+        x = a + b\n+        if x > 0:\n+            if c:\n+                if d:\n+                    if e:\n+                        if f:\n+                            return x\n+        return \"fallback\"\n+    except Exception:\n+        pass\n+    return None\n+\n+def duplicate():\n+    val = 1\n+    val = 1\n+    val = 1\n+    return val",
    "file": "src/example.py",
    "id": "a6dd03549985",
    "location": {
      "end_line": 1,
      "start_line": 1,
      "symbol": "risky"
    },
    "message": "`risky` accepts 6 parameters, which may indicate mixed responsibilities.",
    "recommendation": [
      "Group related params into an object/dataclass.",
      "Split responsibilities into smaller functions."
    ],
    "severity": "MEDIUM",
    "source": {
      "analyzer": "static",
      "pointer": null,
      "rule": "too_many_params"
    },
    "suggested_patch": null,
    "suggested_tests": [
      "Add focused unit tests for each parameter cluster or branch."
    ],
    "title": "Function has too many parameters"
  },
  {
    "category": "structure",
    "confidence": 0.66,
    "context": "@@ -1,3 +1,22 @@\n-def add(a, b):\n-    return a + b\n+def risky(a, b, c, d, e, f):\n+    TODO = \"replace before release\"\n+    try:\n+        x = a + b\n+        if x > 0:\n+            if c:\n+                if d:\n+                    if e:\n+                        if f:\n+                            return x\n+        return \"fallback\"\n+    except Exception:\n+        pass\n+    return None\n+\n+def duplicate():\n+    val = 1\n+    val = 1\n+    val = 1\n+    return val",
    "file": "src/example.py",
    "id": "c7fe2b0e1d4d",
    "location": {
      "end_line": 7,
      "start_line": 7,
      "symbol": null
    },
    "message": "Deep nesting increases branching complexity and maintenance cost.",
    "recommendation": [
      "Use guard clauses/early returns.",
      "Extract nested branches into named helpers."
    ],
    "severity": "MEDIUM",
    "source": {
      "analyzer": "static",
      "pointer": null,
      "rule": "deep_nesting"
    },
    "suggested_patch": null,
    "suggested_tests": [
      "Add branch coverage tests for each nested condition path."
    ],
    "title": "Deep nesting in changed code"
  }
]